#include <stdio.h>
#include "main.h"
/**
 * mul - checks if a character is lower case
 * @a: the character to be checked
 * @b: second number
 * Return: 1 if true and 0 if false
 */

int mul(int a, int b)
{
	return (a * b);
}
