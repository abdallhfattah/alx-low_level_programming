#include"main.h"

/**
 * _abs - gets absolute value of any number.
 * @c: parameter passed
 * 
 * Return: the postive value integer.
 */
int _abs(int c)
{
if(c < 0)
{
return (-c);
}
else
{
return (c);
}
}